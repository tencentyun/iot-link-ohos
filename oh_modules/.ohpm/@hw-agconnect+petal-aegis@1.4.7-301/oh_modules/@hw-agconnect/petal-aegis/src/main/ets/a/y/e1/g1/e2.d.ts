import huks from '@ohos.security.huks'; import { CipherHandler } from "../b2"; import type { CipherAlg, CipherParamsSpec } from "../i1"; export declare class HuksAESAuthCipherHandler extends CipherHandler { private mode; private keyAlias; private paramsSpec; constructor(h47: CipherAlg, mode: huks.HuksKeyPurpose, keyAlias: string, i47: CipherParamsSpec); private initKey; private preprocess; protected doCrypto(f47: Uint8Array[], g47: number[]): Promise<void>; } 