import huks from '@ohos.security.huks'; import { Cipher } from "../f1"; import { HuksAESAuthCipherHandler } from "./e2"; export class HuksAESCipher extends Cipher { constructor(j2, keyAlias, k2) { super(j2); this.encryptHandler = new HuksAESAuthCipherHandler(j2, huks.HuksKeyPurpose.HUKS_KEY_PURPOSE_ENCRYPT, keyAlias, k2); this.decryptHandler = new HuksAESAuthCipherHandler(j2, huks.HuksKeyPurpose.HUKS_KEY_PURPOSE_DECRYPT, keyAlias, k2); } } 