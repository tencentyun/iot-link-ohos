import { Coder, SecureRandom } from "../ets/a/b"; import { AegLogger } from "./c2/d2"; export class SafeRandom { static ohAegRandom(len) { try { return SecureRandom.generateRandom(len); } catch (err) { AegLogger.error(SafeRandom.TAG, 'ohAegRandom parse fail, err code: ' + err.code + ', err msg: ' + err.message); return null; } } static ohAegRandomHex(len) { try { return Coder.HEX.encode(SecureRandom.generateRandom(len)); } catch (err) { AegLogger.error(SafeRandom.TAG, 'ohAegRandom parse fail, err code: ' + err.code + ', err msg: ' + err.message); return null; } } static ohAegRandomNum(len) { try { let f43 = SecureRandom.generateRandom(len); let g43 = f43.join('').replace('-', ''); return g43.substring(0, len); } catch (err) { AegLogger.error(SafeRandom.TAG, 'ohAegRandomNum parse fail, err code: ' + err.code + ', err msg: ' + err.message); return null; } } } SafeRandom.TAG = 'aegis SafeRandom'; 