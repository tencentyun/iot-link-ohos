import cryptoFramework from '@ohos.security.cryptoFramework'; import { CipherHandler } from "../b2"; import type { CipherAlg } from "../i1"; export declare class AsyCipherHandler extends CipherHandler { private mode; private key; constructor(c48: CipherAlg, mode: cryptoFramework.CryptoMode, key: cryptoFramework.Key); private rsaPreProcess; private checkInputLen; protected doCrypto(a48: Uint8Array[], b48: number[]): Promise<void>; } 