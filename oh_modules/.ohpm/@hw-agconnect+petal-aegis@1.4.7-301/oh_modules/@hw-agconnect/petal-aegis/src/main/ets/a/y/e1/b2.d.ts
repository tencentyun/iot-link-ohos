import { DataProcessor } from "../h2"; import type { CipherAlg } from "./i1"; export declare abstract class CipherHandler extends DataProcessor { protected cipherAlg: CipherAlg; constructor(h46: CipherAlg); fromArray(data: Array<Uint8Array>): this; toArray(): Promise<Uint8Array[]>; to(): Promise<Uint8Array>; protected abstract doCrypto(f46: Array<Uint8Array>, g46: Array<number>): Promise<void>; } 